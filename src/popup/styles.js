const style = () => {
  return {
    root: {
      minWidth: 500,
      display: "flex",
      flexDirection: "column"
    },
    pandas: {
      maxHeight: 300,
      overflowY: 'auto',
    }
  };
};

export default style;
